@model Northwind.Mvc.Models.ShippersContactInfo

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>ShippersContactInfo</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">Email</dt>
        <dd class="col-sm-10">@Model.Email</dd>
        <dt class="col-sm-2">Website</dt>
        <dd class="col-sm-10">@Model.Website</dd>
        <dt class="col-sm-2">Phone</dt>
        <dd class="col-sm-10">@Model.Phone</dd>
        <dt class="col-sm-2">Address</dt>
        <dd class="col-sm-10">@Model.Address</dd>
        <dt class="col-sm-2">City</dt>
        <dd class="col-sm-10">@Model.City</dd>
        <dt class="col-sm-2">Country</dt>
        <dd class="col-sm-10">@Model.Country</dd>
        <dt class="col-sm-2">PostalCode</dt>
        <dd class="col-sm-10">@Model.PostalCode</dd>
        <dt class="col-sm-2">Shipper</dt>
        <dd class="col-sm-10">@Model.Shipper</dd>
        <dt class="col-sm-2">CustomerId</dt>
        <dd class="col-sm-10">@Model.CustomerId</dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.ShippersContactInfoId">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
